package net.kevinmendoza.igneouspack.tests;

import com.flowpowered.math.vector.Vector2i;

import net.kevinmendoza.geoworldlibrary.geology.GeologicContainer;
import net.kevinmendoza.igneouspack.main.IntrusiveFactory;

public class TestMain {
/*
	public static void main(String[] args) {
		DebuggerOverride DebuggerOverride = new DebuggerOverride();
		GeologicContainer container = IntrusiveFactory.MakeIntrusiveMap(DebuggerOverride);
		for(int x=0;x<10;x++) {
			for(int z=0;z<10;z++) {
				Vector2i testPoint = new Vector2i(x*100,z*100);
				container.primeGenerationAt(testPoint);
				for(int x2 = x*100;x2<2 + x*100;x2++) {
					for(int z2 = z*100;z2<2 + z*100;z2++) {
						container.getColumnAt(testPoint);
					}
				}
			}
		}
	}*/

}
